<section class="page-section section-spacing">
  <div class="page-width page-width--narrow">
    <div class="breadcrumbs"><a href="{{ routes.root_url }}">Home</a><span>/</span><span>{{ page.title }}</span></div>
    <div class="page-heading"><p class="eyebrow">Ababeel Fashions</p><h1>{{ page.title }}</h1></div>
    <div class="rte page-content">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Main page", "settings": [] }
{% endschema %}
